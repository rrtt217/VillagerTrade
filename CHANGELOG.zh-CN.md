# 更新日志

[English](CHANGELOG.md) · **中文** · [说明文档](README.zh-CN.md)

## v2.1 — 2026-09-08

本轮全面测试（静态检查 + 服务器运行时探针 + 真实客户端点击）后发现并修复：

### 修复

- **数据丢失（严重）** —— `villager_data.txt` 的读取模式比 `SaveVillagerData` 写出的字段多一组，
  导致每次加载都报告 0 个村民；紧接着 `Initialize` 末尾的保存又用内存数据重写文件，把已存
  经验全部抹掉。现在读写可以正常往返，包括表示"从未刷新"的 `-1` 占位值。
- **无限复制刷怪蛋（严重）** —— 合成配方只设置了产物、从未声明材料，因此材料永远不被消耗。
  现在同时设置 `SetIngredient` 与 `SetResult`。
- **多人互相覆盖（高）** —— 交易窗口、村民 ID 与交易选择都是全局的，第二个玩家开窗会清空
  第一个玩家的状态。现按玩家（UUID，无则用名字）分别保存，窗口只清理自己的状态。
- **连交白扣物品（高）** —— Shift+左键会从**两个**输入槽各扣同样的数量，把交易不需要的物品也
  扣掉。现在按每个输入自身的需求量扣减。
- **连交数量溢出（高）** —— `cItem:AddCount` 的参数是 8 位有符号数，超过 ±127 会回绕：64 个
  绿宝石可能被全部扣除而产出为 0。现在改为显式计算数量，并按背包剩余空间封顶，产出分配到
  多个槽位（先叠加已有堆叠，再占用空槽）。背包已满时会提示"背包空间不足"，而不是静默扣掉输入。
- **输出槽可被白拿** —— 当输入不再匹配时，输出槽里的预览物品仍能被拿走。现在该槽由插件独占：
  拒绝放入物品、未付款的预览直接清除（不掉落）、关窗时一并丢弃。
- **窗口尺寸** —— 改为客户端原版村民窗口（3 槽 + 背包 36 槽 = 39 槽）。原来的 10×10 窗口会向
  只有 39 槽的客户端发送 136 个槽位（Cuberite 文档明确警告可能崩溃客户端），并且把背包镜像到
  了错误的槽位。
- **`trades.txt` 物品名** —— `lapis_lazuli` → `lapislazuli`，`chainmail_leggings` →
  `chainmain_leggings`（Cuberite 的官方拼写）。此前这两条会生成空物品，玩家付了钱却拿不到东西。
  现在无法识别的物品名会在控制台告警。
- **热重载** —— 新增 `Info.lua` 并把插件名改为 `VillagerTrade`，与文件夹一致；`reload_plugin`
  恢复可用（此前按名字和按文件夹都失败）。
- **附魔等级** —— `ByXpLevels-(min,max)` 不匹配解析模式，附魔等级恒为 0。
- **其他** —— UUID 为空不再报错；玩家离开时与每 5 分钟自动保存村民数据；关窗只掉落非空的输入
  槽；v1→v2 迁移不再按世界重复执行；删除重复的 `DEBUGLOG` 定义；把数量为 0 的"幽灵物品"
  按空槽处理。

### 新增

- `Info.lua`（插件元数据）与 `settings.ini`（`[Features] EnableVillagerSpawnEggCrafting`）。
- 右键输出槽可在匹配到的多条交易间循环切换，并在聊天栏提示当前选择（`交易 2/4：…`）；
  匹配集合变化时自动重置选择。
- `trades.txt` 中出现无法识别的物品名时输出控制台告警。

### 变更

- 交易窗口不再镜像玩家背包——客户端直接显示真实背包。
- 多条匹配交易的选择方式从"点击槽 30"的临时补丁改为右键输出槽。

## v2 — 2026-08-09

- 村民唯一标识符 `vt-<职业>-<随机字符>`，存放于 CustomName（随世界持久化）。
- 按村民持久化到 `villager_data.txt`（经验 + 上次刷新年龄）。
- v1 → v2 迁移：玩家经验移交给第一个对应职业的村民。
- 交易刷新改为基于世界年龄，而非村民年龄。
- 阻止用命名牌给插件管理的村民改名。
- 村民刷怪蛋合成配方（绿宝石 + 鸡蛋）与 `vtGeneric` 职业的对应交易。

## v1 — 2025-12-26 … 2025-12-28

- 数据驱动的交易定义（`trades.txt` + 解析器）。
- 交易窗口、背包同步、Shift+左键支持。
- 交易经验、解锁等级，以及按玩家的存档（`player_trade_experience.txt`、`player_trades.txt`）。
